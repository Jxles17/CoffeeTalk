{% extends 'base.html.twig' %}

{% block title %}Gestion des articles{% endblock %}

{% block body %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
        th,
    td {
    border: 1px solid rgb(160 160 160);
    padding: 8px 10px;
    }
</style>

<div class="example-wrapper">
    <h1>Gestion des articles</h1>

    <a class="btn btn-primary" href="{{ path('article.create') }}">Créer un article</a>

    <table class="table">
        <thead>
            <tr>
                <th>Titre</th>
                <th>Date et heure</th>
                <th>Description</th>
                <th>Image</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
            {% for article in articles %}
                <tr>
                    <td>{{ article.title }}</td>
                    <td>{{ article.description }}</td>
                    <td><img src="{{ asset('uploads/articles/' ~ article.imageFilename) }}" alt="{{ article.title }}" style="max-width: 100px;"></td>
                    <td>
                        <a class="btn btn-primary btn-sm" href="{{ path('article.edit', {id: article.id}) }}">Modifier</a>
                        <form action="{{ path('article.delete', {id: article.id}) }}" method="post">
                            <input type="hidden" name="_method" value="DELETE">
                            <button class="btn btn-danger btn-sm" type="submit">Supprimer</button>
                        </form>
                    </td>
                </tr>
            {% endfor %}
        </tbody>
    </table>

</div>
{% endblock %}
